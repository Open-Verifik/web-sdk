import { PasswordlessRoutes } from './passwordless-routes';

describe('PasswordlessRoutes', () => {
  it('should create an instance', () => {
    expect(new PasswordlessRoutes()).toBeTruthy();
  });
});
