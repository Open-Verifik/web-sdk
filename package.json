{
  "name": "verifik-sdk",
  "version": "9.6.2",
  "description": "Verifik SDK for JavaScript",
  "main": "sdk",
  "scripts": {
    "app": "live-server --host=localhost --port=8000"
  },
  "dependencies": {
    "live-server": "^1.2.1",
    "dotenv": "16.0.3"
  }
}
